pipeline {
    agent any
    stages {
	    stages {
        stage('git checkout') {
            steps {
                 git branch: 'master', url: 'https://github.com/jitendrapsingh/pipeline1jenkinsfile'
                
            }
        }
	}
}
}
